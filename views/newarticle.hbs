<script>
    function submitArticle() {
        document.getElementById('error').innerHTML = "";

        if (document.getElementById('title').value == "" || document.getElementById('content').value == "") {
            document.getElementById('error').innerHTML = "Missing Fields!";
        } else {
            var data = {
                title: document.getElementById('title').value,
                content: document.getElementById('content').value,
                category: document.getElementById('category').options[document.getElementById('category').selectedIndex].value,
                published: document.getElementById('published').checked
            };

            $.post('newarticle', data, function(response, status) {
                window.location = response.url;
            });
        }
    };

    function display(input) {
        var image = document.getElementById("imagepreview");
        var reader = new FileReader();
        var base64;
        reader.onload = function(e) {
            document.getElementById("uploadstatus").innerHTML = "uploading...";

            base64 = e.target.result
            image.src = base64;

            $.post('/imageupload', {image: base64} , function(response, status) {
                document.getElementById("uploadstatus").innerHTML = "uploaded!";
            });
        }
        
        reader.readAsDataURL(input.files[0]);
    }
</script>

<div class="centered">
    New Article<br><br>
    <form>
        Title<br>
        <input id="title" type="text"><br>
        Image<br>
        <input type="file" accept="image/*" onchange="display(this)"><br>
        <img height="300px" id="imagepreview"><br>
        <div id="uploadstatus"></div><br>
        Content<br>
        <textarea rows="40" cols="100" id="content"></textarea><br>
        Category<br>
        <select id="category">
            <option value="classrooms">Classrooms</option>
            <option value="offices">Offices</option>
            <option value="food">Food</option>
        </select><br>
        Publish<br>
        <input id="published" type="checkbox"><br>
        <br>

        <button type="button" onclick="submitArticle()">Save</button>
        <p id="error"></p>
    </form>
</div>